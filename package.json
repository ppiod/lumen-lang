{
  "name": "lumen",
  "version": "1.0.0",
  "main": "src/index.ts",
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^24.4.0",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.43.0"
  },
  "scripts": {
    "dev": "bun run .",
    "build:exe:win": "bun build . --compile --minify --sourcemap --target=bun-windows-x64 --outfile ./bin/lumen.exe",
    "build:exe:linux": "bun build . --compile --minify --sourcemap --target=bun-linux-x64 --outfile ./bin/lumen",
    "lint": "bunx prettier --write ."
  },
  "type": "module"
}